#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // cout << (-2) % 3 << endl;
    // cout << ((-4) & 1) << endl;
    cout << cos(1.00000000001) << endl;
    // int dp[3][2];
    // int &r = dp[0][1];
    // r = max(r, 3);
    // cout << r << endl;
    // cout << dp[0][1] << endl;
    // cout << (-1) % 2 << endl;
    return 0;
}
