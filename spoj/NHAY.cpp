#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ii pair<int, int>

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    char c;
    while (cin >> c) {
        if (c == '\n') break;
        cout << "yay" << endl;
    }

    return 0;
}
