#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ii pair<int, int>
const int mxN = 2e5;
double a[mxN + 5], t[mxN + 5];
int N;
double T;
int lAmt, lId = -1, rAmt, rId = -1;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> N >> T;
    for (int i = 1; i <= N; i++) {
        cin >> a[i] >> t[i];
        // if (t[i] <= )
    }

    return 0;
}
