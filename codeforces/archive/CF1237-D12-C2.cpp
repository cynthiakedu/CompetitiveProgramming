#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ii pair<int, int>
int N, arr[50005][3];
bool vis[500005];
vector<ii> ans;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> N;
    for (int i = 1; i <= N; i++) cin >> arr[i][0] >> arr[1] >> arr[2];

    for (int i = 1; i <= N / 2; i++) {
    }

    return 0;
}
