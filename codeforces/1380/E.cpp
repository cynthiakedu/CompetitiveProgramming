#include <bits/stdc++.h>
using namespace std;
int T, N, X;

void solve(vector<int> &v) { v[0] = 1; }

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  vector<int> w = {0};
  solve(w);
  cout << w[0] << endl;

  return 0;
}
